/**
 * GitHub contribution calendar.
 *
 * Contribution data comes from a third-party API (github-contributions-api),
 * not from GitHub directly — no token needed, but it can be down or rate
 * limited, which the component handles.
 */
export const githubConfig = {
  username: "LianCr",
};
